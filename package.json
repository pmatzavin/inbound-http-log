{
  "name": "fast-http-log",
  "version": "1.0.0",
  "description": "Fast(est?) HTTP logs",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "tap test/*.js --100 --cov",
    "test-snapshot": "TAP_SNAPSHOT=1 tap test/*.js",
    "posttest": "tap --coverage-report=lcov"
  },
  "keywords": [
    "http",
    "logs",
    "logger",
    "fast",
    "fastest",
    "incoming",
    "inbound",
    "outcoming",
    "outbound",
    "request",
    "response"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^4.15.0",
    "prettier": "^1.10.2",
    "tap": "^11.1.0"
  },
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.5.1"
  }
}
